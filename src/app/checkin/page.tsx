import React from "react";
import CheckIn from "../../components/CheckIn"; // Ajuste o caminho para o componente CheckIn

export default function CheckInPage() {
    return (
        <div>
            <CheckIn />
        </div>
    );
}
